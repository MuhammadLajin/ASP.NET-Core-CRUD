@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@model day1core.Models.Student
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<form method="post">
    <div class="form-group">
        @Html.HiddenFor(n => n.StId)

        @Html.LabelFor(n => n.StFname)
        @Html.TextBoxFor(n => n.StFname, new { @class = "form-control" })
        @Html.ValidationMessageFor(n => n.StFname, "", new { @class = "text-danger" })
    </div>
    <div class="form-group">
        @Html.LabelFor(n => n.StLname)
        @Html.TextBoxFor(n => n.StLname, new { @class = "form-control" })
        @Html.ValidationMessageFor(n => n.StLname, "", new { @class = "text-danger" })
    </div>
    <div class="form-group">
        @Html.LabelFor(n => n.StAddress)
        @Html.TextBoxFor(n => n.StAddress, new { @class = "form-control" })
        @Html.ValidationMessageFor(n => n.StAddress, "", new { @class = "text-danger" })
    </div>
    <div class="form-group">
        @Html.LabelFor(n => n.StAge)
        @Html.TextBoxFor(n => n.StAge, new { @class = "form-control" })
        @Html.ValidationMessageFor(n => n.StAge, "", new { @class = "text-danger" })
    </div>

    <div class="form-group">
        <label for="squareSelectDept">Department</label>
        <select name="DeptId" class="form-control input-pill" id="squareSelectDept">
            @foreach (day1core.Models.Department item in ViewBag.dept)
            {
                @*if (item.DeptId == int.Parse(Html.LabelFor(n => n.DeptId).ToString()))
                {
                    <option value="@item.DeptId" selected>@item.DeptName</option>
                }
                else
                {
                    <option value="@item.DeptId">@item.DeptName</option>
                }*@

                <option value="@item.DeptId">@item.DeptName</option>
            }
        </select>
    </div>
    <input type="submit" value="save" />

</form>